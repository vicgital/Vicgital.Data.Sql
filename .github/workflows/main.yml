name: Vicgital.Data.Sql


on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

env:
  GH_PACKAGE_USERNAME: ${{ secrets.GH_PACKAGE_USERNAME }}
  GH_PACKAGE_TOKEN: ${{ secrets.GH_PACKAGE_TOKEN }}
  startupProject: "src/Vicgital.Data.Sql/Vicgital.Data.Sql.csproj"

jobs:
  publish:
    runs-on: ubuntu-latest
    permissions:
      packages: write
      contents: read
    steps:

      - name: Publish Nuget Package
        uses: vicgital/cicd/actions/dotnetNugetPublish@main
        with:
          startupProject: ${{ env.startupProject }}
          githubPublishPackageToken: ${{ secrets.GITHUB_TOKEN }}